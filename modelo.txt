#include <iostream>
#include <locale>
#include <windows.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Portuguese");
    SetConsoleCP(1252);
    SetConsoleOutputCP(1252);

    // float;
    // int;
    // string;
    // char[1];
    // vetor[1];
    // matriz[row][col];

    // struct Name{
    // } strName[quantity];

    cout << "";
    cin >> i;

    return 0;
}
