#include <iostream>
#include <locale>
#include <windows.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Portuguese");
    SetConsoleCP(1252);
    SetConsoleOutputCP(1252);

    /*
    float;
    int;
    string;
    char[5];
    */

    int vetor[DIM] = {10, 15, 20, 25, 30};

    cout << "";
    cin >> i;

    return 0;
}
